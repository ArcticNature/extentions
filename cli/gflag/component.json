{
  "name": "ext.cli.gflag",
  "type": "c++",

  "deps": [
    "core.interface.lifecycle",
    "core.registry.cli-parser"
  ],

  "inject": ["core.bin.daemon"],
  "libs": ["gflags"],

  "targets": {
    "debug":   { "type": "lib" },
    "release": { "type": "lib" },
    "test":    { "type": "lib" }
  }
}
